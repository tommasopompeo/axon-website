# AXON — Design System

Tema scuro, grandi titoli display, spaziature generose, micro-animazioni allo scroll.

> Questo documento descrive **esattamente ciò che è pubblicato sul sito** (`app/`, `components/`, `design/tokens.css`). Riconciliato con il codice il 2026-08-18 (passata disclosure legali + SEO/CWV/a11y — vedi §10 e §15): ogni valore sotto è verificato contro l'implementazione reale, non aspirazionale. Se in futuro qualcosa in questo file e il codice divergono, il codice resta la fonte di verità — aggiorna questo file, non il contrario.

## 1. Identità
- **Font: Urbanist** (Google Fonts, `next/font/google`). Pesi 400–800.
- **Logo**: SVG wordmark **bianco**, "X" **rossa**. File in `/public/logo.svg`.
- **Palette**: rosso brand `#db181b`, near-black, bianco. Token sotto.

## 2. Color tokens (`design/tokens.css` e `tokens.json`)
| Token | Valore | Uso |
|---|---|---|
| `--bg` | `#0A0A0B` | sfondo pagina (default) |
| `--bg-black` | `#000000` | sfondo sezioni "pure black" intenzionali (Hero pagine B2B, FAQ, ScienceSection, VideoSection, ecc. — leggermente più scuro di `--bg`, differenza non percepibile ma tenuta come scelta deliberata) |
| `--bg-elevated` | `#121214` | sezioni alternate (`elevated` prop su `Section`) |
| `--surface` | `#161618` | card scure (form panel, box contatti) |
| `--surface-2` | `#1E1E21` | card hover / input |
| `--border` | `rgba(255,255,255,0.08)` | bordi sottili |
| `--border-strong` | `rgba(255,255,255,0.14)` | bordi hover |
| `--text` | `#FFFFFF` | titoli/testo primario su sfondo scuro |
| `--text-muted` | `rgba(255,255,255,0.66)` | paragrafi su sfondo scuro |
| `--text-subtle` | `rgba(255,255,255,0.45)` | note/caption su sfondo scuro |
| `--text-on-white-muted` | `rgba(10,10,11,0.62)` | paragrafi/sottotitoli su card e sezioni **bianche** (Shop, use-case cards, PartnerSection) |
| `--brand` | `#db181b` | CTA, X del logo, accenti |
| `--brand-hover` | `#b6151a` | hover CTA |
| `--brand-soft` / `--brand-glow` | `rgba(219,24,27,.10)` / `.22` | badge, icone, tint |
| `--header-bg` | `rgba(8,8,9,0.72)` + `backdrop-blur(12px)` | header sticky |

Tema: **dark only**, con due eccezioni intenzionali che passano a testo nero su sfondo chiaro: l'hero video (home) e le superfici bianche "commerciali" (Shop, card use-case, PartnerSection).

**Grigi Tailwind fuori-token eliminati** (consolidamento 2026-08-14): `TestimonialCarousel` usava `border-gray-800`/`text-gray-400` (ora `--border-strong`/`--text-muted` — differenza cromatica impercettibile, ±3–7/255) e `ContestiBento` usava `border-gray-100`/`text-gray-800` sui 4 stat box (ora il bordo `rgba(0,0,0,.06)` di `Card variant="white"` — impercettibile; la caption `text-gray-800`, un quasi-nero `#1f2937` senza token equivalente, è invece migrata a `--text-on-white-muted` accettando uno **schiarimento visibile**, per scelta esplicita anziché lasciare un grigio non tokenizzato).

## 3. Tipografia
Tutti i titoli (h1–h6) renderizzano a **weight 400** — è una scelta visiva deliberata (display leggero, non bold) applicata in modo uniforme su tutto il sito. Non esiste più una regola globale che la impone: il peso vive esplicitamente in ciascuna classe utility qui sotto, insieme a font-size/line-height/letter-spacing, cosicché una classe che dichiara un peso è sempre quella che lo applica davvero. Elementi che *non* sono titoli (es. il prezzo su `ProductCard`, le label "Richiesta ricevuta"/"Messaggio inviato") possono legittimamente restare bold — non sono soggetti a questa scala.

Una singola scala "display" copre hero H1 **e** i titoli di sezione principali — è la stessa dimensione ripetuta, non due tier diversi:
- **`.text-display`** (`--fs-display`): `clamp(3.25rem, 7.0vw, 5.2rem)`, weight 400, line-height 1.04, tracking `-0.02em`.
  Usato per: ogni hero H1 (tutte le 8 pagine) **e** i titoli H2 di sezione principali su Home/Perché AXON/Come Funziona (es. "Migliora il tuo benessere a 360°", "Chi lo indossa, lo racconta", "Le basi scientifiche del movimento passivo").
- **`.text-h2`** sottosezione (`--fs-h2`): `clamp(2rem, 4vw, 3rem)`, weight 400, line-height 1.1, tracking `-0.02em`. Usato per titoli di sottosezione più contenuti (es. "Una tecnologia, molte professioni" su Professionisti/Aziende, "Domande frequenti" — quest'ultimo **intenzionalmente** più piccolo perché vive in una colonna fissa di 360px).
- **`.text-h3`** card: `1.375rem` (`--fs-h3`), weight 400, tracking `-0.01em`.
- **`.text-lead`**: `1.25rem` (`--fs-lead`), line-height 1.6, weight 400 — paragrafi introduttivi sotto i titoli.
- **Eyebrow** (etichetta pill sopra i titoli): `0.8125rem`, uppercase, tracking `0.10em`, weight 600 — non è un titolo (è un `<p>`), quindi resta bold legittimamente.
- **Stat**: due tier onesti, non un token unico condiviso — bento e risultati usano dimensioni realmente diverse in produzione, non drift accidentale (bento vive in tile compatte di una griglia densa, risultati è l'unico dato in evidenza dentro un pannello accordion aperto):
  - **`--fs-stat-bento`**: `clamp(1.8rem, 3.2vw, 2.4rem)` — i 4 stat box di `ContestiBento` (+42%, +28%, +30%, +21%).
  - **`--fs-stat-results`**: `clamp(2rem, 4vw, 2.75rem)` — il dato in evidenza di `ResultsAccordion` (Perché AXON).
  - Sono `<span>`/`<p>`, non titoli, e restano bold/extrabold di proposito.
- Body (`--fs-body`): `1.0625rem`, line-height 1.6, colore `--text-muted` (o `--text-on-white-muted` su sfondo bianco).
- Caption/legale: `0.8125rem` (`--fs-caption`), `--text-subtle`.

Le quattro classi (`.text-display` / `.text-h2` / `.text-h3` / `.text-lead`, definite in `app/globals.css`) sono l'unico posto che bundla size + line-height + letter-spacing + weight per la scala titoli: qualunque heading che condivide uno di questi pattern usa la classe invece di ripetere i valori inline. Un paio di titoli con dimensioni realmente uniche (es. "Comodo. Resistente. Versatile." in `AxonFeaturesSection`, i pillar H3 in `ScienceSection`) mantengono uno style inline dedicato — non essendoci una regola globale a forzare il peso, questi restano a `font-weight: 400` per eredità naturale (nessun elemento antenato dichiara bold).

## 4. Spaziatura, raggi, layout
- **Section padding verticale** (`--section-y`): `clamp(4rem, 6.5vw, 6rem)` — applicato da tutte le `<Section>` (incl. Applicazioni: AxonFeaturesSection, WearMethodSection).
- **Container**: max-width **`1440px`** (`--container`, mappato su `max-w-container` in `tailwind.config.ts`), gutter fisso `px-6` (24px) → `md:px-12` (48px) → `lg:px-16` (64px). Un solo container in tutto il sito — usato anche da header/footer, non solo dal contenuto. `Container`/`Header`/`Footer` usano tutti la classe Tailwind `max-w-container`, non un valore arbitrario `max-w-[1440px]` duplicato in tre punti.
- **Header height** (`--header-h`): `64px` — altezza reale dell'header (`h-16`). Usato dagli hero full-viewport (`min-h-[calc(100vh-var(--header-h))]`) per riempire esattamente lo spazio sotto l'header sticky.
- **Raggi**: card e immagini feature a schermo intero → `var(--radius-lg)` = **24px** (`rounded-lg` nella config Tailwind, già mappato al token); input/select → `var(--radius-md)` = 16px; bottoni/eyebrow → pill `999px`. Scala a due livelli, mai mischiata sulla stessa superficie.
- **Bordi**: card scure `1px solid var(--border)`; card bianche `1px solid rgba(0,0,0,0.06)`; hover rinforza a `--border-strong`.

## 5. Componenti
- **Header**: sticky, blur, logo a sx, nav al centro/dx, CTA rossa "Acquista AXON" a dx. Mobile: hamburger → overlay scuro, padding orizzontale fisso `px-6` (stesso del Container, non un token separato). Entrambe le CTA (desktop e mobile-menu) passano da `Button` — vedi sotto.
- **Button** (`components/ui/Button.tsx`) — 4 varianti (`primary` / `secondary` / `ghost` / `white`) × 4 size (`sm` / `md` / `lg` / `custom`):
  - `primary` — rosso pieno, hover schiarisce + `translateY(-2px)`.
  - `secondary` — trasparente con bordo, hover riempie `--surface-2` + `translateY(-2px)`.
  - `ghost` — link testuale con freccia, hover schiarisce il colore (nessun transform).
  - `white` — bianco/nero, per CTA su sezioni scure che richiedono massimo contrasto (FAQ "Hai altre domande?", submit dei form su Professionisti/Aziende/Contatti). Nessun lift al hover (solo schiarimento bg).
  - `size="custom"` — non applica alcuna classe di padding/text-size: il chiamante fornisce l'intero set via `className`. Esiste perché sovrascrivere un'utility Tailwind di size con un'altra (es. `px-6` → `px-5`) non ha un ordine di cascata affidabile; usata dalle 3 CTA "one-off" che non combaciano con `sm`/`md`/`lg` (Header desktop, Header mobile-menu, hero Home). `onClick` è supportato (necessario per chiudere il menu mobile al tap).
  - **Nota accessibilità**: la CTA "Acquista AXON" del Header desktop è alta **38px** (`min-h-[38px]`, dimensione originale preservata durante la migrazione a `Button`) — sotto il minimo `btn-base` di 44px e sotto il target-size WCAG consigliato. Le altre due CTA (mobile-menu ~50px, hero Home ~52px) superano naturalmente 44px. Non corretto in questa passata di consolidamento (solo refactor, zero cambi visivi) — da rivedere se l'accessibilità del target diventa una priorità.
- **PageHero** (`components/ui/PageHero.tsx`) — scaffold hero full-viewport condiviso da Home/Applicazioni/Come Funziona/Perché AXON: `Section` (min-height `100vh - var(--header-h)`, flex-centered) → media di sfondo (`video`, `Image` o nessuna, sempre `-z-10`) → overlay scuro opzionale (`overlayOpacity`, 0–1, reso solo se > 0) → `Container` → riga flex (`flex-col lg:flex-row lg:items-center lg:justify-between`) con colonna testo `max-w-4xl` + `decor` opzionale. Props: `media` (`{type:'video',src}` · `{type:'image',src,alt}` · `{type:'none'}` — nessun elemento di sfondo, mostra il `background` della `Section` sottostante), `background` (forwarded a `Section`: `'default'|'elevated'|'black'|'white'`, rilevante solo con `media.type:'none'`), `overlayOpacity`, `tone` (`'light'` testo bianco default · `'dark'` testo nero, usato solo dalla Home), `children` (contenuto colonna testo), `decor` (2026-08-18, opzionale — elemento decorativo reso **in flusso** come sibling della colonna testo nella stessa riga flex, mai in absolute: a qualunque larghezza la colonna testo si restringe/va a capo per fargli spazio invece di sovrapporlesi mai. Solo la Home lo usa, per il disco AXON — vedi §6), `cta` (opzionale, reso come sibling della riga — solo la Home lo usa, per il bottone sotto il fold).
- **MediaFrame** (`components/ui/MediaFrame.tsx`) — il frame media con glow condiviso da `StickyScrollApplicazioni`, `WearMethodSection` e `ResultsAccordion`: `aspect-square`, `max-w-lg lg:max-w-xl xl:max-w-[620px]`, `rounded-lg` (24px), `shadow-[0_0_50px_rgba(255,255,255,.08)]`, `border-white/10`. Il posizionamento esterno (`justify-*`, `order-*`) resta nel chiamante perché differisce per layout.
- **Card** (`components/ui/Card.tsx`) — superficie condivisa per box di contenuto (form panel, box contatti, tile use-case, stat box, trust box). Due varianti: `surface` (scura, default — hover `border → --border-strong` / `background → --surface-2` sempre attivo, via classe CSS `.card-surface`, nessun lift) e `white` (prop `hover` opzionale, aggiunge solo `shadow-md → shadow-lg`). Usata da Professionisti/Aziende/Contatti/Home (`ContestiBento`, `HowItsDoneSection` — le 2 card testo nere sopra/sotto le immagini, prima hand-rolled `bg-black border-gray-800` senza hover, ora `variant="surface"` con l'hover in dote)/Shop (trust box).
- **Section** (`components/ui/Section.tsx`) — prop `background`: `default` (trasparente, eredita `--bg`) · `elevated` (`--bg-elevated`) · `black` (`--bg-black`) · `white` (`#ffffff`). Sostituisce il vecchio pattern `className="bg-black" style={{background:'var(--bg-black)'}}` ripetuto in ~18 punti.
- **Eyebrow pill**: bordo sottile, uppercase.
- **Accordion**: 4 implementazioni indipendenti (per contesti diversi: FAQ generica via `ui/AccordionItem`; scroll-jacked in WearMethodSection/StickyScrollApplicazioni; deep-dive in ScienceSection) — tutte allineate sulla stessa firma di movimento: `duration 0.35s`, `ease cubic-bezier(0.22,1,0.36,1)`.
- **Testimonial carousel**: autoplay, swipe mobile, frecce prev/next.
- **Form**: input su `--surface-2`, label sopra, focus ring `--brand`, confirm inline (via `Card`). Layout condiviso a due colonne per le pagine con form (`grid grid-cols-1 gap-10 lg:grid-cols-[minmax(0,380px)_minmax(0,1fr)] lg:gap-20 lg:items-start`): colonna intro da 380px a sinistra, form `1fr` a destra, campi accoppiati via `grid sm:grid-cols-2 gap-5` — usato da Professionisti, Aziende e (dal 2026-08-17) Contatti, che nella colonna sinistra mostra 3 FAQ invece del solo intro (vedi §10). **Textarea autogrow (2026-08-17)**: niente resize manuale (`resize: none`), `min-height` 120px invariato, crescita automatica verso il basso col contenuto via `field-sizing: content` (Baseline Newly available da giugno 2026) più fallback JS nel componente `Textarea` (`height = scrollHeight` in un `useLayoutEffect` sul valore controllato, attivo solo dove `CSS.supports('field-sizing','content')` fallisce — niente listener manuali, quindi nessun conflitto con React né col reset post-invio); crescita cappata a `max-height: 50vh`, oltre subentra lo scroll interno (`overflow-y: auto`). Nota: con l'autogrow l'altezza a vuoto è governata dal solo `min-height` (120px) — l'attributo `rows` non incide più. **Consegna email (2026-08-17, rivista 2026-08-18)**: i 3 form (Contatti/Professionisti/Aziende) inviano a **Web3Forms** direttamente dal browser via `submitContact(source, fields)` in `lib/contact.ts` — unico modulo che parla col provider. Il modulo costruisce oggetto (`[Sito AXON — {Pagina}] Nuova richiesta da {nome}`) e corpo strutturato (una riga per campo, ordine fisso: Pagina, Nome, Email, campi specifici della pagina, Messaggio per ultimo — i campi opzionali vuoti vengono omessi) e POSTa a `https://api.web3forms.com/submit` con `NEXT_PUBLIC_WEB3FORMS_KEY`; l'email del mittente è impostata come reply-to, così rispondere da `info@axon-tech.it` funziona senza altro setup. **Perché client-side e non una API route**: il piano free di Web3Forms rifiuta con 403 qualunque chiamata server-side/proxy (la richiesta deve partire dal browser per la loro spam prevention — server-side richiederebbe piano a pagamento + IP safelist, impraticabile su Vercel dove gli IP delle function sono dinamici). La prima implementazione (2026-08-17, commit `63728c0`) usava una route `/api/contact` con chiave solo-server ed è stata sostituita per questo motivo; la chiave `NEXT_PUBLIC_` è visibile nel sorgente ma Web3Forms la definisce esplicitamente pubblica e sicura nel client — consegna solo verso l'email dell'account. Validazione e honeypot `_honey` restano client-side nei form (più il campo `botcheck` di Web3Forms inviato vuoto). Web3Forms è il provider scelto *oggi* perché consegna sull'email dell'account senza bisogno del DNS del dominio (non ancora configurato); il giorno in cui `axon-tech.it` avrà il DNS pronto, lo swap a **Resend** (API server-side, chiave privata) reintroduce una route interna chiamata da `submitContact` — `lib/contact.ts` è l'unico modulo da toccare, i 3 form non cambiano; la versione server-side del codice (validazione server, provider isolato) è recuperabile dalla history al commit `63728c0`. Nessun ramo di successo finto: il vecchio placeholder `FORM_ENDPOINT === '#'` che simulava l'invio è stato rimosso — se l'invio fallisce (env mancante, rete, rifiuto del provider) il client mostra lo stesso stato di errore server (`serverError`) già esistente, mai una conferma non veritiera.
- **Footer**: scuro (`--bg-black`), banda di contenuto unica, **nessuna riga inferiore** (separatore rimosso il 2026-08-17). Layout a lg+: blocco brand a sinistra (logo, tagline, icone social condizionate + fine stampa legale) e, a destra, un cluster compatto delle tre colonne di link (Azienda / Navigazione / **Informazioni Legali**) in riga flessibile con `gap-x-12` (`flex-shrink-0`, non si comprime mai) — molto più stretto della vecchia distribuzione a griglia full-width, si legge come un unico gruppo — il cui bordo destro (colonna Informazioni Legali) combacia col limite destro del container, lo stesso a cui si allinea la CTA dell'header. Il blocco brand si stira all'altezza del cluster (flex `items-stretch` sul contenitore + `justify-between` sul blocco) così l'ultima riga della fine stampa cade in linea con "Shop", ultima voce di Navigazione — nessun margine "a occhio". Il vincolo `max-w-xs` che limitava l'intero blocco brand è stato spostato sul solo wrapper logo/tagline/social: la fine stampa (`lg:max-w-md`) resta fuori da quel limite, cosicché a lg+ si allarga al proprio "fit-content" naturale (~420px, la riga più lunga, il P.IVA) e sta sempre su due righe invece di spezzarsi su tre — il gap minimo blocco-cluster è sceso a `gap-x-8` (era `gap-x-16`, un semplice pavimento mai raggiunto dalla spaziatura reale tranne che nella fascia stretta 1024–~1110px) per lasciarle lo spazio necessario senza mai intaccare il cluster. **Informazioni Legali** (Privacy `/privacy`, Cookie `/cookie`, Termini `/termini`) è ora una colonna vera e propria, stessa gerarchia tipografica delle altre (h3 `text-xs uppercase tracking-widest` + item `text-sm link-muted`), non più la vecchia riga di link `text-xs` in fondo. **Navigazione** rispecchia esattamente le 5 voci dell'header (`Header.tsx`, stesso ordine e stessa label "Perchè AXON") + Shop; **Azienda** è Contatti (`/contatti`) + `info@axon-tech.it` (mailto) + la ragione sociale "Axon-Tech S.r.l." come testo semplice (non è più un link `#`: non ha una destinazione). Sotto lg: blocco brand, poi le tre colonne (stack singola su mobile, 2 colonne su md), poi per ultima la fine stampa legale in fondo al footer — posizionata via due render condizionati a breakpoint (`hidden lg:block` / `lg:hidden`) dello stesso testo anziché duplicazione manuale, cosicché un solo nodo per volta resta nel DOM/esposto agli screen reader. Fine stampa (`text-xs`, `link-subtle`): "Axon è un dispositivo medico di Classe I. Leggere le istruzioni per l'uso." + "© {anno} Axon-Tech S.r.l. — Via Verdi 73, 31100 Treviso (TV). P.IVA IT05577370264.". La colonna **Prodotto** (AXON KIT/Come si usa/FAQ/Testimonianze) è stata rimossa il 2026-08-17 — nessuna voce esclusiva che non fosse già raggiungibile altrove. Icone social: rese solo quando `SOCIAL.<piattaforma>` in `lib/links.ts` non è più `'#'` (rendering condizionato sul valore, non commentato/hardcoded) — finché tutti e tre restano placeholder, il blocco social non renderizza nulla, evitando link morti; impostare l'URL reale in `lib/links.ts` è un one-line restore, nessuna modifica a `Footer.tsx`.

**Nota storica — `ResultsAccordion` "elevated"**: la sezione "Forza" su Perché AXON passava `elevated` a `ResultsAccordion` per alternare lo sfondo a `--bg-elevated`, ma un `style` inline che sovrascriveva la logica interna di `Section` rendeva la prop di fatto inerte — le tre sezioni (Equilibrio/Forza/Mobilità) hanno **sempre** renderizzato tutte nero puro. Con la migrazione al prop `background` la prop `elevated` è stata rimossa (invece di "aggiustarla in silenzio", che avrebbe cambiato visivamente la sezione Forza): tutte e tre restano `background="black"`, identiche a prima.

## 6. Anatomia HOME (ordine sezioni)
1. Header (sticky)
2. **Hero** — sfondo **bianco pieno** (`Section background="white"`), testo e CTA in **nero**; a destra, in flusso accanto al testo (`PageHero` `decor`, da `lg`, nascosto sotto), il disco AXON che ruota (`/hero-video-disc.mp4`)

**Hero — disco disaccoppiato dal full-bleed (2026-08-18)** — prima il video (`hero-video.mp4`, 3840×2160: sfondo bianco pieno a sinistra + disco che ruota in una regione piccola e fuori-centro a destra) copriva l'intera `Section` via `object-cover`, quindi la posizione/dimensione dello schermo del disco dipendeva dal rapporto larghezza/altezza del **viewport intero**, non del contenuto — a certe combinazioni di risoluzione il disco finiva sotto il testo (entrambi posizionati indipendentemente, nessuna relazione layout tra loro), e su mobile il crop lo tagliava quasi del tutto fuori. Fix: il video è stato croppato (ffmpeg, bbox del disco campionato su 48 frame lungo l'intero loop per restare valido a ogni fase di rotazione, +8% di margine) in un nuovo asset quadrato 960×960 senza audio, `/public/hero-video-disc.mp4` (2.0MB → 481KB), reso in un box `w-[clamp(220px,26vw,420px)] aspect-square` **in flusso** dentro la riga flex di `PageHero` (vedi prop `decor` sopra) invece che come sfondo assoluto — a qualunque larghezza il testo si restringe per fargli spazio, non può più sovrapporglisi. Sotto `lg` il disco sparisce del tutto (troppo stretto per una seconda colonna leggibile) tramite `<source media="(min-width:1024px)">`: sotto quella soglia nessuna sorgente combacia, quindi il browser non scarica nessun byte del video (verificato via network panel — prima, un semplice `hidden` via CSS avrebbe comunque scaricato l'mp4 in background). Nessun `poster`: lo sfondo del video ritagliato è lo stesso bianco pieno della `Section`, quindi il riquadro vuoto prima della decodifica del primo frame è indistinguibile dallo stato caricato. Asset vecchi (`hero-video.mp4`, `hero-video-poster.jpg`) rimossi, nessun riferimento rimasto nel codice.
3. **Migliora il tuo benessere a 360°** (ContestiBento) — bento grid: corsa (col 1 span 2), box dati bianchi (4 sub-box con stat rosse: +42%, +28%, +30%, +21%), nuoto (col 2 riga 2), palestra (col 3 span 2)
4. **Indossa Axon come preferisci** (HowItsDoneSection) — griglia 2 colonne, metodi di applicazione
5. **Per come ti muovi, ogni giorno** (TargetCarousel) — griglia statica con 4 card immagine
6. **Chi lo indossa, lo racconta** (TestimonialsSection + TestimonialCarousel)
7. **Domande frequenti** (FaqSection)
8. Footer

## 7. Anatomia APPLICAZIONI
1. Hero — immagine `hero_applicazioni.jpg`, overlay 30%, titolo display
2. StickyScrollApplicazioni — scroll-pinned, 7 contesti d'uso, frame immagine 24px radius + glow `shadow-[0_0_50px_rgba(255,255,255,.08)]`
3. AxonFeaturesSection — sfondo bianco, layout assoluto attorno al disco AXON, stesso glow sul drop-shadow del prodotto
4. WearMethodSection — scroll-pinned, 2 metodi di applicazione, stesso frame immagine di (2)

## 8. Anatomia COME FUNZIONA
1. Hero — immagine `come_funziona_hero.jpg`, overlay 30%, titolo display
2. VideoSection — video embed `video_come_funziona.mp4`
3. ScienceSection — 3 pillar scientifici (Meccanotrasduzione / Vibrazioni / Nanomateriali), ciascuno con testo intro + accordion "Approfondimento tecnico" + immagine sticky; sezione riferimenti bibliografici in fondo

## 9. Anatomia PERCHÉ AXON
1. Hero — immagine `hero_perche_axon.jpg`, overlay 30%
2. Paragrafo intro (sfondo `--bg-black`)
3. ResultsAccordion × 3 sezioni, tutte su sfondo nero puro: Equilibrio (destra) / Forza (sinistra) / Mobilità (destra) — ogni metrica appare una sola volta; grafici in `/public/grafici-perche-axon/1–7.png`; frame immagine 24px radius via `MediaFrame`, identico ad Applicazioni (vedi §5)
4. PartnerSection — sfondo bianco, titolo display nero, 2 loghi reali (`/logo1.png`, `/logo2.png`), padding verticale standard (`--section-y`, non maggiorato)

## 10. Anatomia SHOP / PROFESSIONISTI / AZIENDE / CONTATTI
Pagine "utility": hero H1 in `--fs-display` (stessa scala del resto del sito), corpo su `--bg-black`, form con `Card` component per stato di successo e box informativi. Professionisti/Aziende aggiungono una griglia di 6 tile use-case (`Card variant="white"`, hover non definito — solo colore/icona).

**Contatti riallineato (2026-08-17)** — entrambe le `Section` (hero `id="top"` e form `id="contatto"`) sono passate a `background="black"`: prima l'hero ereditava il trasparente `--bg` di default e il form viveva su `background="elevated"`, uniche pagine utility a non usare nero pieno. Lead paragraph, spaziatura dei campi form (`gap-5`, griglia `sm:grid-cols-2`) e il Card di stato "successo" (`p-8`, label bold a `--fs-h3`, corpo `--fs-body`/`--text-muted`) erano già identici a Professionisti/Aziende, nessuna modifica necessaria lì.

**Box "Contatti diretti" rimosso (2026-08-17)** — il vecchio layout specchiato (form a sinistra `1fr` + Card con email/indirizzo/Instagram a destra 360px) è stato eliminato. Email e indirizzo restano comunque raggiungibili altrove sul sito (Footer, colonna Azienda e riga legale — vedi §5), quindi nessuna informazione è andata persa, solo la sua duplicazione su questa pagina. Il passaggio intermedio a colonna singola (`max-w-xl`) è stato superato lo stesso giorno dal riallineamento al pattern B2B (vedi sotto).

**Form Contatti sul pattern B2B + FAQ (2026-08-17)** — la sezione `id="contatto"` usa ora la stessa griglia dei form Professionisti/Aziende (`grid grid-cols-1 gap-10 lg:grid-cols-[minmax(0,380px)_minmax(0,1fr)] lg:gap-20 lg:items-start`, `background="black"`, `!pt-0` perché segue direttamente l'hero). Colonna sinistra: h2 `.text-h2` "Domande frequenti" (posizionato come gli intro B2B) seguito da 3 FAQ in accordion (`ui/AccordionItem`, una aperta per volta come nella FaqSection della Home): "Come si usa?", "Cosa contiene il Kit?", "Come si lava il capo?", selezionate **per id** da `lib/faqs.ts` — il modulo condiviso in cui è stato estratto l'array FAQ della Home (con id kebab-case per voce; `FaqSection` ora lo importa: domande e risposte vivono in un solo posto, zero stringhe duplicate). Colonna destra: il form dentro `Reveal delay={0.1}`, nome+email accoppiati su `sm:grid-cols-2`, oggetto e messaggio full-width, consenso e presentazione errori invariati, submit `Button variant="white" size="lg"` come Professionisti (prima era `primary`). Set di campi invariato (nome, email, oggetto, messaggio, consenso, honeypot). Su mobile lo stack segue l'ordine DOM: titolo + FAQ prima, form dopo — come le pagine B2B.

**Pagine legali (`/privacy`, `/cookie`, `/termini`, 2026-08-17)** — variante prose-only dello scaffold utility, senza `PageHero` e senza form: hero identico (`Section background="black"` → H1 `.text-display` → intro `.text-lead` su `--text-muted` → caption "Ultimo aggiornamento" in `--fs-caption`/`--text-subtle`), poi un secondo `Section` (`className="!pt-0"`) con il corpo testo. Ogni sezione numerata è un blocco `LegalSection` condiviso (`components/legal/LegalSection.tsx`): H2 `.text-h2` + paragrafi `--fs-body`/1.7/`--text-muted`, estratto per evitare di ripetere 3× lo stesso markup. Liste puntate: `list-disc pl-5` (stesso pattern delle fonti bibliografiche in `ScienceSection`). Nessuna interattività — le tre pagine sono Server Component, non `'use client'`. `/cookie` documenta l'assenza di cookie/tracker/embed di terze parti (verificato via audit del codice: nessun analytics, nessuno script di tracciamento, `lib/links.ts` con endpoint ancora placeholder `#`) e, coerentemente con le Linee guida del Garante, non introduce alcun banner di consenso.

**Larghezza corpo testo — full-width, non 70ch (2026-08-18)** — a differenza del resto del sito "prose" (es. il paragrafo intro di `app/perche-axon/page.tsx`, che resta `max-w-[70ch]` per la leggibilità), il corpo delle tre pagine legali riempie l'intera larghezza del `Container` senza alcun cap: nessun `max-w-*` sul wrapper (`flex flex-col gap-14` e basta). Scelta esplicita del cliente, non un default del design system — l'adattività a ogni breakpoint viene gratis dal gutter responsive del `Container` stesso (`px-6 → md:px-12 → lg:px-16`, v. §4), senza bisogno di query aggiuntive.

**`/termini` — disclosure obbligatorie art. 2250 c.c. / cod. cons. / D.Lgs. 70/2003 / D.Lgs. 137/2022 (2026-08-18)** — pagina riscritta da 10 a 11 sezioni numerate. Nuova sezione 1 "Il Venditore": blocco identificativo completo (denominazione, sede legale, CF/P.IVA, ufficio e numero del Registro delle Imprese, REA, capitale sociale con parte versata, PEC) che soddisfa insieme l'art. 2250 c.c. (indicazioni societarie obbligatorie) e l'art. 49 cod.cons. (identità del professionista nei contratti a distanza) — un commento JSX in cima al blocco spiega perché non va duplicato altrove. Nuova sezione 3 "Informazioni sul dispositivo medico" (classe, marcatura CE, rinvio al fabbricante di §1, riga Aut. Min., DoC "disponibile su richiesta delle autorità competenti" — mai pubblicata per intero, MDR art. 6(3)). Sezione 5 "Come si conclude l'acquisto" riscritta per l'art. 12 D.Lgs. 70/2003 (fasi tecniche del checkout Shopify, correzione errori pre-conferma, archiviazione del contratto). Sezione 7 "Diritto di recesso" ora include il modello di recesso (Allegato I parte B cod.cons.) riprodotto per intero in un box bordato. Sezione 8 "Garanzia legale di conformità" riscritta sul testo vigente post D.Lgs. 170/2021 (nessuna denuncia entro 2 mesi, prescrizione 26 mesi, presunzione 1 anno — la vecchia regola dei 2 mesi era obsoleta e va evitata in ogni revisione futura). Sezione 10 aggiunge le indicazioni ADR (art. 141-sexies cod.cons.) e **rimuove** ogni riferimento alla piattaforma ODR UE (dismessa il 20 luglio 2025, obbligo di link decaduto il 19 gennaio 2025 — non reintrodurre il link). Nessuna menzione di SCIA (nessun obbligo di pubblicazione una volta accolta dal SUAP). Tre valori non deducibili dal repo (REA, capitale sociale con parte versata, PEC) sono resi come placeholder `{{TOKEN}}` — vedi sotto.

**Placeholder `{{TOKEN}}` (2026-08-18)** — `components/legal/Placeholder.tsx`: componente condiviso, un `<mark>` con bordo tratteggiato rosso e monospace, usato ovunque manchi un valore legale reale. Tre token ancora in uso, tutti in Termini §1: `{{REA}}`, `{{CAPITALE_SOCIALE}}`, `{{PEC}}`. Nessun hook interno: funziona sia in Server Component (`app/termini/page.tsx`) sia in Client Component (se servisse altrove).

**Data autorizzazione ministeriale — valore reale, non più placeholder (2026-08-18)** — `AUT_MIN_DATE` (`lib/legal.ts`) sostituisce il vecchio `{{DATA_AUT_MIN}}`: fonte unica condivisa da Footer, Shop punto vendita e Termini §3 (importata nei tre file, aggiornarla solo lì se l'autorizzazione cambia). Il commento in `lib/legal.ts` ricorda ai manutentori che l'autorizzazione copre un solo messaggio, validità 24 mesi (12 se rivendica novità), e che nuovi claim richiedono una nuova domanda, non un rinnovo.

**Dicitura dispositivo medico — formula standard art. 26 D.Lgs. 137/2022 (2026-08-18)** — sostituita ovunque la vecchia riga "Axon è un dispositivo medico di Classe I. Leggere le istruzioni per l'uso." con la formula standard "AXON è un dispositivo medico CE. Leggere attentamente le avvertenze e le istruzioni per l'uso. Aut. Min. del {AUT_MIN_DATE}.": nella fine stampa del Footer (site-wide, invariata nella posizione) e, nuova, al punto vendita in `ShopContent.tsx` (sotto la griglia delle due product card, prima della sezione "Cosa include il Kit") — è il punto che un reviewer della pubblicità sanitaria controlla per primo.

**404/500 on-brand (2026-08-18)** — `app/not-found.tsx` e `app/error.tsx` (quest'ultimo `'use client'`, obbligatorio per l'error boundary di App Router) condividono lo stesso scaffold: `Section background="black"` centrata verticalmente, H1 `.text-display`, due CTA (`Button` primary + secondary). `not-found.tsx` è statico; `error.tsx` logga l'errore in console (nessun servizio di error-reporting configurato) e espone un bottone "Riprova" che chiama `reset()`.

**CTA Shopify disabilitate (2026-08-18)** — `ProductCard.tsx`: finché `href` è il placeholder `'#'` (`lib/links.ts`), il bottone "Acquista" è sostituito da un bottone `disabled` "Presto disponibile" + una riga di testo con mailto di fallback, mai un `<a href="#">`. Nessuna modifica richiesta quando gli URL Shopify reali vengono impostati in `lib/links.ts` — il componente torna automaticamente al link attivo.

## 11. Motion
- **`lib/motion.ts`** — sorgente unica per `EASE` (`[0.22, 1, 0.36, 1]`, mirror di `--ease` in `design/tokens.css`) e `DURATION` (`ui` 0.2s, `uiSlow` 0.35s, `reveal` 0.6s, mirror di `--dur`). Tutti gli usi Framer Motion di easing/duration lo importano invece di ridichiararlo — nessun'altra costante `EASE` locale né `easeOut`/`easeInOut` stray nel codebase.
- **Reveal**: due trigger, due meccanismi.
  - `trigger="mount"` (contenuto above-the-fold, es. hero) → **puro CSS**: classe `.reveal-mount` + `@keyframes reveal-mount` in `app/globals.css` (`opacity 0→1`, `y 24→0`, `duration var(--dur)`, `ease var(--ease)`, delay via custom property `--reveal-delay`). Il componente `Reveal` renderizza un `<div>` semplice, non un `motion.div`: niente `opacity:0` SSR-ato in attesa di JS, l'animazione parte anche se lo script non idrata. `prefers-reduced-motion` è gestito via `@media` (animazione disattivata, contenuto visibile a `opacity:1`).
  - `trigger="scroll"` (default) → resta Framer Motion (`whileInView`, `duration DURATION.reveal`, `ease EASE`, viewport once): richiede comunque JS per sapere quando l'elemento entra in viewport, quindi CSS non porterebbe benefici. `RevealGroup` + `RevealItem` per stagger figli (sempre Framer).
- **Hero pinning** (Whoop-style, tutte e 4 le pagine con `PageHero`: Home, Applicazioni, Come funziona, Perché AXON) — categoria di movimento a sé, **puro scroll nativo, nessuno scroll-jacking né alterazione della velocità**. Meccanica:
  - `PageHero` (`components/ui/PageHero.tsx`) è `position: sticky` (`top: var(--header-h)`, `min-h` `100vh - var(--header-h)`, `z-0`): l'hero resta appuntato mentre il resto della pagina gli scorre sopra. Nessuno spacer div — l'elemento sticky mantiene la propria altezza nel layout, quindi **CLS resta 0**.
  - Ogni pagina avvolge il contenuto post-hero in un **content wrapper** `position: relative; z-10` con background opaco (`var(--bg)`): è ciò che slitta sopra l'hero coprendolo. `z-10 > z-0` (hero) `< z-50` (Header, sempre sopra a tutto). Il wrapper `relative` non interferisce con lo sticky annidato di `StickyScrollApplicazioni` (lo sticky è ancorato al viewport, non al wrapper).
  - **Static outer wrapper (obbligatorio)**: ogni pagina avvolge hero + content wrapper in un `<div>` statico senza classi. Motivo: alla navigazione client-side il router di Next.js scrolla verso il **primo nodo DOM della pagina** ma **salta gli elementi `position: sticky/fixed`** (`shouldSkipElement` in `layout-router`) e gli elementi a rect zero (es. gli `<script>` di JsonLd) — senza il wrapper salterebbe l'hero sticky e farebbe `scrollIntoView` sul content wrapper, atterrando con la pagina già scrollata oltre l'hero. Un div statico full-height non viene saltato, non crea stacking context e non vincola lo sticky dell'hero (lo contiene per l'intera altezza pagina).
  - **Fade**: overlay nero `<HeroFade>` (`components/ui/HeroFade.tsx`, classe base `.hero-fade` in `app/globals.css`, `z-20`) dentro l'hero, `opacity 0 → 0.85` sul primo viewport (`100vh`) di scroll, guidato da **Framer Motion** (`useScroll` → `useTransform`): solo `opacity` (resta sul compositor) e via `MotionValue` → scrive nel DOM senza re-render React, **nessun setState per-frame**. `pointer-events: none` → la CTA/il testo dell'hero restano cliccabili finché non vengono fisicamente coperti.
  - **Perché non CSS scroll-driven** (`animation-timeline: scroll()`): sarebbe la strada più leggera (zero JS), ma lo scroller root ha `overflow-x: clip` (per tagliare l'overflow orizzontale dei glow decorativi) e questo impedisce a `scroll(root)` di tracciare in Chromium, pur con `CSS.supports` a `true` — il fade non partirebbe mai. Framer legge `window.scrollY` direttamente ed è immune. Solo `HeroFade` è client component; `PageHero` resta server.
  - **Degradazione**: `prefers-reduced-motion` (via `useReducedMotion`) rende l'overlay statico a `opacity: 0` — nessun fade legato allo scroll; lo stacking naturale (il wrapper copre comunque l'hero) resta. Coerente con come `.reveal-mount` degrada il movimento.
- **Header nav** — stato attivo/hover in CSS, non JS: classe `.nav-link` (colore `--text-muted` → `--text` su `:hover`/`:focus-visible`, transition `color 0.2s var(--ease)`), niente più `onMouseEnter`/`onMouseLeave`. Il link della pagina corrente riceve `aria-current="page"` (via `usePathname`) che porta un piccolo indicatore rosso brand: un puntino sotto il link su desktop (`.nav-link--desktop::after`), a sinistra dell'etichetta nel menu mobile (`.nav-link--mobile::before`).
- **Hover bottoni** (`primary`/`secondary`): `translateY(-2px)`, `duration 0.2`; **`:active`** riporta a `translateY(0)` (press state). Card: **nessun lift** — bento/use-case cards (`Card variant="white" hover`) usano solo `shadow-md → shadow-lg` al hover; le card scure (`Card` default, `variant="surface"`) e i tile di `TargetCarousel` hanno uno stato hover dedicato via classi CSS (`.card-surface`, `.target-tile`): `border-color → --border-strong`, `background → --surface-2`, `0.2s var(--ease)` — le proprietà base vivono nella classe CSS (non in inline style) proprio perché un valore inline non può mai essere sovrascritto da una regola `:hover`.
- Accordion/expand-collapse (tutte le varianti): `duration 0.35`, `ease cubic-bezier(0.22,1,0.36,1)` (via `lib/motion.ts`).
- Crossfade immagini (scroll-jacked sections): `opacity duration 0.5` + `scale transition 1s` — categoria di movimento distinta, più lenta, per transizioni "editoriali" tra fotografie.
- Carousel: `TargetCarousel` — nonostante il nome, dalla conversione a griglia (vedi §6) non è più un carousel: griglia statica CSS (`grid`, 1→2→4 colonne), nessuno scroll-snap/scroll-jacking. `TestimonialCarousel` = crossfade orizzontale via Framer (`AnimatePresence mode="wait"`); il contenitore della quote è un `motion.div layout="size"` che assorbe il cambio di altezza tra quote di lunghezza diversa con una transizione morbida (`DURATION.uiSlow`, `EASE`) invece di uno scatto secco.
- **Rispettare `prefers-reduced-motion`**: disattivare y-translate e autoplay (verificato su `Reveal`/`RevealItem`/`ui/AccordionItem`/`ResultsAccordion`/ScienceSection `DeepDive`/`TestimonialCarousel`).

## 12. Responsive
- Mobile-first. Breakpoint Tailwind: `sm 640 · md 768 · lg 1024 · xl 1280`.
- Nav: hamburger sotto `md`; inline da `md`. Overlay mobile con lo stesso padding orizzontale del Container (`px-6`).
- Griglie: 1 col (mobile) → 2 col (`md`) → 3 col (`lg`).
- Tipografia fluida via `clamp()`.
- Target tap ≥ 44px (eccezione nota: Header desktop CTA, 38px — vedi §5).

## 13. Accessibilità
- Contrasto AA su testo.
- Focus visibile (ring `--brand`) su tutti gli elementi interattivi.
- `alt` su tutte le immagini; label esplicite sui form.
- HTML semantico (`header/nav/main/section/footer`, heading order corretto).
- **Skip link** (2026-08-18): primo elemento del `<body>` in `app/layout.tsx`, punta a `#contenuto-principale` (id su `<main>`). Fuori schermo (`translateY` negativo) finché non riceve focus da tastiera, poi appare come pill brand sopra l'header — classe `.skip-link` in `globals.css`.
- **Accordion/carousel accessibili da tastiera** (2026-08-18): `StickyScrollApplicazioni` e `WearMethodSection` usavano `<div onClick>` per i titoli cliccabili — irraggiungibili da tastiera. Convertiti in `<button>` veri con `aria-expanded`, stesso pattern già in uso da `ResultsAccordion`/`ui/Accordion`. `ui/Accordion.tsx` (`AccordionItem`, condiviso da FAQ Home/Contatti/Shop) ha ricevuto `aria-controls`/`aria-labelledby`/`role="region"` per collegare esplicitamente bottone e pannello.
- **Contrasto testo inattivo negli accordion scroll-jacked** (2026-08-18): i titoli non attivi di `StickyScrollApplicazioni`/`WearMethodSection`/`ResultsAccordion` erano `text-white/35` (2.99:1 su nero, sotto il minimo AA 3:1 per testo "large" ≥24px) — portati a `white/40` (~3.4:1). `--text-subtle` (caption/fine stampa) era `rgba(255,255,255,.45)` (4.42:1 a 12px, sotto il minimo AA 4.5:1 per testo normale) — portato a `.48` (4.68:1). Verificato con Lighthouse: 0 violazioni `color-contrast` su Home/Shop/Applicazioni dopo il fix (prima: 1 su ciascuna).

## 15. SEO, sicurezza e Core Web Vitals (2026-08-18)
- **Metadata per route**: ogni pagina ha `title`/`description` propri (niente più duplicati generici), `alternates.canonical` relativo a `metadataBase` (`app/layout.tsx`) e un blocco `openGraph` con `title`/`description`/`url`. Il `title` delle pagine con `template: '%s | Axon'` nel layout root passa solo il nome pagina (es. `'Applicazioni'`, non `'Applicazioni - AXON'`) per evitare il brand duplicato nel tab del browser.
- **Structured data** (`components/JsonLd.tsx`, tre export invece di un unico blob): `OrganizationJsonLd` in `app/layout.tsx` (site-wide), `ProductsJsonLd` in `app/shop/page.tsx` (`Product`+`Offer` con prezzo/valuta reali da `knowledge/product.md`; `availability: PreOrder` finché `SHOPIFY_*_URL` sono `'#'` — passare a `InStock` quando Shopify va live; nessun campo review/rating inventato), `FaqJsonLd` in `app/page.tsx` (`FAQPage` generato dallo stesso array `lib/faqs.ts` che renderizza l'accordion — zero drift tra markup visibile e dato strutturato).
- **Sitemap/robots**: invariati (`app/sitemap.ts`, `app/robots.ts`) — già coprivano tutte le route indicizzabili; verificato via crawl dello static build che sitemap e route effettivamente raggiungibili combaciano esattamente (11/11).
- **Pagine 404/500 on-brand**: v. §10.
- **Security headers** (`next.config.ts`, nuovo `async headers()`): HSTS (`max-age=63072000; includeSubDomains; preload`), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` minima (nega camera/microfono/geolocalizzazione/payment, nessuna di queste API è usata dal sito), `X-Frame-Options: DENY` + `Content-Security-Policy: frame-ancestors 'none'` (unica direttiva CSP adottata — provabilmente non-breaking perché riguarda solo l'embedding del sito in frame altrui; una CSP completa richiederebbe nonce/hash per gli script inline di Next e non è stata introdotta in questa passata).
- **CTA "Acquista" disabilitate finché Shopify non è live**: v. §10 (`ProductCard.tsx`).
- **LCP — video hero (era solo Home)**: il poster del video hero è l'elemento LCP ma il browser scarica l'attributo `poster` a priorità bassa (non esiste `fetchpriority` sul `poster` HTML). `PageHero.tsx` chiama `preload(media.poster, {as:'image', fetchPriority:'high'})` (API `react-dom`) quando `media.type === 'video'` — path che solo la Home usava (`hero-video.mp4`), non le altre 3 hero (sempre state `media.type:'image'`, priorità già data da `<Image priority>`). Misurato all'epoca: `hero-video-poster.jpg` ridotto da 3840px (312KB) a 2048px (56KB), LCP Home 8.7s → 3.0s. **Dal 2026-08-18 la Home non passa più `media.type:'video'`** (vedi nota in §6: sfondo hero ora `media.type:'none'` + `Section background="white"`) — quel ramo `preload` in `PageHero.tsx` resta nel codice per le pagine future che potessero averne bisogno, ma oggi non è più invocato da nessuna delle 4 hero. Lo sfondo bianco pieno paga zero fetch/decodifica, quindi l'LCP della Home è oggi tipicamente l'H1 stesso — un miglioramento ulteriore rispetto al 3.0s già misurato, non ancora re-testato con Lighthouse dopo questo cambio.
- **LCP — logo header/footer**: `logo.svg` pesa 465KB (incorpora raster PNG in base64 per il filtro drop-shadow) e viene caricato con `priority` su ogni pagina — competeva per banda con l'elemento LCP reale. Aggiunto `public/logo-nav.png` (144×144, 6.5KB, rasterizzato dall'SVG stesso, canale alpha preservato): usato da `Header`/`Footer` al posto di `logo.svg`, che resta in `/public` solo per branding/JSON-LD.
- **LCP — Shop**: la griglia prodotti usava `RevealGroup`/`RevealItem` (Framer Motion `whileInView`), che SSR-a le card a `opacity:0` fino all'idratazione — essendo above-the-fold, ritardava l'LCP. Passata a `Reveal trigger="mount"` (CSS puro, stesso criterio già documentato in `Reveal.tsx` per contenuto above-the-fold) + `priority` sulla prima immagine della prima card (`ProductCard` ha ora una prop `priority`). Risultato: LCP Shop 7.0s → 2.6s.
- **LCP — Applicazioni**: nessun fix dedicato oltre al logo/poster condivisi — il miglioramento (7.1s → 2.7s) viene interamente dal logo `logo-nav.png`, che l'hero-immagine di questa pagina contendeva.
- **Numeri finali (Lighthouse mobile emulato, `next build && next start`, throttling di lab — non rappresentativi del campo reale ma comparabili prima/dopo)**:

  | Pagina | LCP prima → dopo | CLS | Perf. score | A11y score |
  |---|---|---|---|---|
  | Home | 8.7s → 3.0s | 0 | 0.75 → 0.94 | 0.96 → 1.00 |
  | Shop | 7.0s → 2.6s | 0 | 0.77 → 0.97 | 0.96 → 1.00 |
  | Applicazioni | 7.1s → 2.7s | 0 | 0.77 → 0.97 | 0.96 → 1.00 |

  FCP invariato (~0.8s) e TBT invariato (≤30ms) su tutte e tre — il collo di bottiglia era interamente sul path critico dell'immagine LCP, non su JS bloccante. CLS era già 0 ovunque prima di questa passata (hero pinning senza spacer div, v. §11) e resta 0.
- **Crawl del build statico**: script Node ad-hoc su `next start` — 11/11 route interne rispondono 200, un solo `<h1>` per pagina, nessun salto di livello negli heading (corretti due salti h1→h3 aggiungendo un `<h2 className="sr-only">` in `StickyScrollApplicazioni` e `ShopContent`), zero `href="#"` residui (le uniche due CTA Shopify placeholder sono bottoni `disabled`, non link), sitemap e route crawlate combaciano esattamente.

## 14. Performance
Gli asset in `/public/` sono stati ricompressi il 2026-08-10: **224MB → 24MB** (-89%). Foto opache esportate in PNG (Canva) → JPEG q82–85, ridimensionate quando il pixel-count superava di molto la dimensione di rendering reale (es. `applicazioni/1–7.jpg`, `corsa.jpg`, `hero_perche_axon.jpg`). PNG mantenuto solo dove necessario: `axon_no_bkg.png` e `logo2.png` (canale alpha/trasparenza), `grafici-perche-axon/1–7.png` (grafici con testo/linee sottili — la JPEG introdurrebbe artefatti). `video_come_funziona.mp4` re-encodato H.264 CRF23 a parità di risoluzione 1080p: 75MB → 8.3MB, qualità visivamente indistinguibile dall'originale.

**Asset-loading fix (2026-08-16)** — audit su caricamento immagini/video:
- **`sizes` sui frame `MediaFrame`**: `StickyScrollApplicazioni` e `WearMethodSection` avevano `<Image fill>` senza `sizes` (default `100vw` → il browser scarica sempre la variante full-width anche se il frame non supera mai 620px, vedi `MediaFrame` in §5). Aggiunto `sizes="(min-width: 1280px) 620px, (min-width: 1024px) 576px, 512px"`, che rispecchia esattamente i breakpoint `max-w-lg lg:max-w-xl xl:max-w-[620px]` del frame. Verificato via network panel: a 800px di viewport la variante richiesta scende da `w=1920` a `w=1080`.
- **`priority` rimosso** dall'immagine `axon-shell-sewn.jpg` in `HowItsDoneSection` (sotto il fold): competeva per banda con l'hero video in fase di LCP.
- **Poster frame per i video hero**: `hero-video.mp4` e `video_come_funziona.mp4` non avevano `poster`, quindi il primo paint era un riquadro vuoto finché il video non decodifica il primo frame. Generati `hero-video-poster.jpg` e `video-come-funziona-poster.jpg` (ffmpeg, primo frame, JPEG q82, stessa risoluzione del sorgente — 3840×2160 e 1920×1080) e collegati rispettivamente a `PageHero` (nuova prop opzionale `media.poster`, usata da `HeroSection`) e al `<video>` di `VideoSection`.
- **Logo header**: l'asset `logo.svg` ha un canvas intrinseco quadrato (`width="2000" height="2000"`, viewBox `1500×1500`) nonostante il wordmark visivo sia più largo che alto — i `width`/`height` passati a `next/image` (144×48) non corrispondevano né al rapporto reale del file né al box realmente renderizzato (che è sempre stato ~48×48, sia prima che dopo questa fix: verificato via `getBoundingClientRect`, nessuna regressione visiva). Questo disallineamento props↔intrinseco generava sia il warning "width or height modified, but not the other" sia il preload "not used" collegato (`priority` genera un `<link rel=preload>` dimensionato sul rapporto dichiarato, che quindi non corrispondeva al srcset realmente scelto). Fix: `width={48} height={48}` (rapporto reale) + `className="h-auto w-auto max-h-10 md:max-h-12"`. Verificato pulito su build di produzione (`next build && next start`) su Home/Applicazioni/Come Funziona — zero warning in console. In `next dev` un residuo "preloaded but not used" può ancora comparire in modo incostante: è un artefatto del doppio-render di React Strict Mode in sviluppo (doppio fetch di `logo.svg` osservato in dev, assente in build di produzione), non un problema di codice.
- **Lazy-loading selettivo**: in `StickyScrollApplicazioni`, dei 7 frame impilati (crossfade via opacity, tutti nello stesso box) solo `activeIndex ± 1` hanno `loading="eager"`, gli altri `loading="lazy"`. Nota: essendo tutti posizionati `inset-0` nello stesso box visibile (nascosti solo via `opacity`), l'IntersectionObserver nativo del browser li considera comunque "in viewport" e il risparmio di banda reale è limitato — l'attributo è comunque corretto semanticamente e non regressivo.
- **Hero `sizes` + `deviceSizes` cap (2026-08-17)**: `PageHero` renderizza il media di sfondo con `<Image fill>` e, fino a questa fix, nessun `sizes` — il default `100vw` risolveva comunque alla variante più larga disponibile, ma senza un `sizes` esplicito Next.js non sa che l'immagine è realmente full-viewport e i `deviceSizes` di default arrivano fino a `3840` (pensato per display 2x/3x su viewport 4K), quindi un normale monitor 2x da ~1920px richiedeva la variante `w=3840` — molto oltre la risoluzione realmente renderizzata. Aggiunto `sizes="100vw"` esplicito su `PageHero` (Home/Applicazioni/Come Funziona/Perché AXON condividono lo scaffold, vedi §5) e ridotto `images.deviceSizes` in `next.config.ts` a `[640, 750, 828, 1080, 1200, 1920, 2048]` (tetto `2048` invece di `3840`). Verificato via network panel: le richieste hero passano da `w=3840` a `w=2048`; nessuna morbidezza visibile a risoluzione desktop su una foto full-bleed.
